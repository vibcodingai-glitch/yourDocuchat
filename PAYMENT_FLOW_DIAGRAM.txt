# 🔄 COMPLETE PAYMENT FLOW DIAGRAM

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                    USER JOURNEY - PAYMENT FLOW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────┐
│  Step 1: USER HITS LIMIT (3 uploads/transcripts)           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 2: Click "Upload" or "Extract Transcript"             │
│  → UpgradeModal.tsx opens                                   │
│  → Shows: "Upload/Transcript Limit Reached"                 │
│  → Button: "Upgrade to Pro"                                 │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 3: Click "Upgrade to Pro"                             │
│  → Modal closes                                              │
│  → navigate('/checkout')                                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 4: Checkout.tsx page loads                            │
│  → Shows: "$9/month" pricing                                │
│  → Shows: Feature list                                       │
│  → Button: "Subscribe Now"                                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 5: Click "Subscribe Now"                              │
│  → POST to localhost:3001/api/create-checkout               │
│  → Body: { userId, userEmail, priceId }                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 6: Backend (server.js) receives request              │
│  → stripe.checkout.sessions.create()                        │
│  → Creates Stripe checkout session                          │
│  → Returns: { url: "https://checkout.stripe.com/..." }     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 7: Frontend redirects to Stripe                       │
│  → window.location.href = data.url                          │
│  → User sees Stripe Checkout page                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 8: User enters payment details                        │
│  → Card: 4242 4242 4242 4242 (test)                        │
│  → Expiry: 12/25                                             │
│  → CVC: 123                                                  │
│  → Click "Pay"                                               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 9: Stripe processes payment                           │
│  → Validates card                                            │
│  → Creates subscription                                      │
│  → Payment successful ✅                                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ├──────────────────────────────┐
                              │                              │
                              ▼                              ▼
┌───────────────────────────────────┐  ┌──────────────────────────────┐
│  Step 10a: Stripe sends webhook   │  │  Step 10b: User redirected   │
│  → POST to backend                 │  │  → success_url               │
│  → /api/stripe-webhook            │  │  → /payment-success          │
│  → Event: checkout.session.       │  └──────────────────────────────┘
│    completed                      │                  │
└───────────────────────────────────┘                  │
                  │                                    │
                  ▼                                    │
┌───────────────────────────────────┐                  │
│  Step 11: Backend handles webhook │                  │
│  → Extract userId from metadata   │                  │
│  → Update Supabase:               │                  │
│    user_usage.is_pro = true       │                  │
│  → Log: "User upgraded to Pro!"   │                  │
└───────────────────────────────────┘                  │
                  │                                    │
                  └────────────────┬───────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 12: PaymentSuccess.tsx loads                          │
│  → Shows: "Welcome to DocuChat Pro!"                         │
│  → refreshUsage() called                                     │
│  → UsageContext fetches new Pro status                       │
│  → isPro becomes true                                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 13: Click "Get Started"                               │
│  → navigate('/dashboard')                                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 14: Dashboard loads with Pro status                   │
│  → Header.tsx re-renders                                     │
│  → Shows: "∞" instead of "3"                                │
│  → Pro badge appears                                         │
│  → canUpload = true (no limit)                              │
│  → canTranscribe = true (no limit)                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  🎉 SUCCESS! User can now:                                  │
│  ✅ Upload unlimited documents                              │
│  ✅ Extract unlimited transcripts                           │
│  ✅ See Pro badge                                           │
│  ✅ Enjoy premium features                                  │
└─────────────────────────────────────────────────────────────┘


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                    TECHNICAL COMPONENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Frontend (React):
├── UpgradeModal.tsx ────────→ Shows when limit reached
├── Checkout.tsx ────────────→ Payment page
├── PaymentSuccess.tsx ──────→ Success page
├── UsageContext.tsx ────────→ Manages Pro status
└── Header.tsx ──────────────→ Displays usage stats

Backend (Express):
├── POST /api/create-checkout → Creates Stripe session
└── POST /api/stripe-webhook ─→ Handles payment events

External Services:
├── Stripe ──────────────────→ Payment processing
└── Supabase ────────────────→ Database (user_usage table)

Database (Supabase):
user_usage table:
├── user_id ─────────────────→ UUID (link to auth.users)
├── document_count ──────────→ INTEGER (upload count)
├── transcript_count ────────→ INTEGER (transcript count)
├── is_pro ──────────────────→ BOOLEAN (false → true on payment)
├── created_at ──────────────→ TIMESTAMP
└── updated_at ──────────────→ TIMESTAMP

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                    STATE CHANGES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before Payment:
├── is_pro: false
├── documentCount: 0-3
├── transcriptCount: 0-3  
├── canUpload: true (if < 3)
├── canTranscribe: true (if < 3)
└── Header: "3 / 3" or "2 / 3"

After Payment:
├── is_pro: true ✅
├── documentCount: [same]
├── transcriptCount: [same]
├── canUpload: true (always!)
├── canTranscribe: true (always!)
└── Header: "∞" + Pro badge

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
